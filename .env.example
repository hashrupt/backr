# =============================================================================
# Database
# =============================================================================
# For Docker local dev: postgresql://postgres:postgres@localhost:5432/backr
# For Supabase: Use Transaction mode URL (port 6543) for DATABASE_URL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/backr"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/backr"

# =============================================================================
# Keycloak Authentication (Canton Network)
# =============================================================================
KEYCLOAK_URL="http://localhost:8080"
KEYCLOAK_REALM="backr"
KEYCLOAK_CLIENT_ID="backr-api"

# Vite frontend Keycloak config
VITE_KEYCLOAK_URL="http://localhost:8080"
VITE_KEYCLOAK_REALM="backr"
VITE_KEYCLOAK_CLIENT_ID="backr-web"

# =============================================================================
# API Configuration
# =============================================================================
VITE_API_URL="http://localhost:4001"
CORS_ORIGIN="http://localhost:5173"

# Canton Scan Proxy API (for entity enrichment)
CANTON_SCAN_PROXY_URL=https://cantara.validator.canton.hashrupt.com/api/validator
CANTON_SCAN_PROXY_TOKEN=your-api-token-here

# A/B Site Configuration
# Each environment deploys two sites (A and B) with different DAML models.
# Dev also supports dual-site for Canton UI development.
SITE_VARIANT=A
NEXT_PUBLIC_SITE_VARIANT=A
DAML_MODEL_VERSION=1.0
NEXT_PUBLIC_DAML_MODEL_VERSION=1.0

# Canton ledger integration (currently mocked)
# Site A and B point to different Canton endpoints
CANTON_LEDGER_URL=
# CANTON_SCANNER_API_URL=

# Groups.io API (parked - for future use)
# GROUPS_IO_API_TOKEN=your-groups-io-token
